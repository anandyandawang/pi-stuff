# Curious

An extension that encourages the AI agent to embrace curiosity. Instead of making assumptions about vague requirements, the agent is prompted to use the `ask_user` tool to get clarification.

## Installation

Add as a pi package or copy the extension into `.pi/extensions/`.
